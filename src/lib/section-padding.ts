/** Standard horizontal padding for page sections. */
export const sectionPaddingX = "px-6 lg:px-16";

/** Standard vertical padding for page sections. */
export const sectionPaddingY = "py-24 lg:py-32";

/** Standard padding for most content sections. */
export const sectionPadding = `${sectionPaddingX} ${sectionPaddingY}`;
